# DeepFlow RL 审稿修订执行记录

> 开始日期：2026-09-16  
> 维护范围：代码、实验配置、训练与评估产物、图表数据链、论文和审稿回复。  
> 当前阶段：P0、P1 原始结果追踪及 P1 策略比较重构已完成；三种子 PPO 训练与独立测试重算的可复现执行链正在实施。

## 使用规则

- 每项修订开始前记录目标、影响范围、方案依据和待验证项；实施期间在关键决策或
  风险出现时实时补充。
- 每项修订完成后必须记录：当前进度、本次改动、具体实现方式、采用的方法或措施、
  验证命令及结果、仍存风险、下一步和对应本地 Git 提交。
- 数值、图表和结论只能追溯到统一仿真器生成的结构化结果；不得手工修改数据以维持
  既有结论，也不得将模拟 `tok/s` 记为真实部署测量。
- 交接或恢复工作前先阅读本文件、`AGENTS.md` 和修订方案，再更新本文件的交接状态。

## 总体进度

| 优先级 | 修订包 | 状态 | 说明 |
| --- | --- | --- | --- |
| P0 | A：范围、术语与表述 | 未开始 | 本阶段按委托范围不修改论文正文。 |
| P0 | B：统一仿真器语义修正 | 已完成 | 严格本地、远程无投机、位置上限和 PPO 观测已修订。 |
| P0 | B：回归测试 | 已完成 | 新增语义和观测回归测试，并运行既有阶段测试。 |
| P1 | C：结果追踪与图表可复现性 | 部分完成 | 论文实验已具备原始 JSON/CSV 输出链；等待有效 PPO 工件后生成重算结果。 |
| P1 | D：随机压力仿真 | 未开始 | 依赖 P0 后端。 |
| P1 | F：Static/Heuristic/PPO/Oracle | 部分完成 | 固定策略、启发式和逐场景 Oracle 已重构；PPO 三种子重训与独立结果待执行。 |
| P2 | E：规模、接受率与上下文审计 | 未开始 | 依赖 P0/P1 结果。 |
| P2 | G：论文与回复信 | 未开始 | 本阶段明确不处理。 |

## 修订条目

### 2026-09-16 - 建档与持续性规则

**当前进度**：已阅读根目录 `AGENTS.md`、`DeepFlow RL 审稿修订方案（整合优化版）.md`，并对照了论文源 `paper/FGCS-main.tex`、统一后端 `engine/simulator.py`、PPO 环境 `rl/envs/flow_env.py` 及现有实验入口。已建立本记录并在 `AGENTS.md` 加入强制实时更新和交接规则。

**本次内容与实现方式**：新增本文件作为长期审稿修订账本；在 `AGENTS.md` 的“审稿意见修订规则”后新增“修订记录与交接”章节，明确开始、实施、完成和跨对话接手时必须记录的信息。

**采用的方法或措施**：以 `engine/simulator.py` 为唯一仿真事实来源，先追踪论文的模式与指标声明再检查环境和实验脚本。初步确认待修复问题包括：`P=num_layers, K=0` 仍生成激活值通信；`P=0, K=0` 使用最小 Token 请求载荷但现有命名为 cloud-only；位置长度未在模拟前拒绝；单步 PPO 环境的 `last_throughput` 在动作选择前无信息量。

**验证情况**：已完成文档内容核查。初始 `git status --short` 受已有 Git LFS 临时对象访问拒绝影响而失败：工作树的 Git 元数据位于 `E:\workspace\python\DeepFlow-RL\.git\worktrees\DeepFlow-RL`，错误指向 `E:\workspace\python\DeepFlow-RL\.git\lfs\tmp\1681372254`。尚未修改或删除该临时对象，也未覆盖他人改动；后续提交前需在不破坏 LFS 产物的前提下恢复 Git 状态检查与提交能力。

**仍存风险**：当前无法由常规 `git status` 确认工作树是否已有未提交修改，且可能阻塞本地完整提交。该问题必须先安全诊断；不将其误判为本次代码改动。

**对应本地提交**：`7edd647`（建立审稿修订执行记录与交接规则；本条目将在提交修订后保持同一提交说明）。

**下一步**：实现 P0 仿真语义和位置可行性测试，最后调整 PPO 观测并验证训练/评估接口。

### 2026-09-16 - P0 统一仿真语义与 PPO 观测

**当前进度**：P0 的代码与实验层修订已完成，修改了 `engine/simulator.py`、`rl/envs/flow_env.py`、`run_paper_experiments.py`、`eval_phase5.py` 和两份新增回归测试；未修改论文正文、回复信或最终图表。

**本次内容与实现方式**：

- 在 `DeepFlowSimulator` 内新增统一的模式标签、通信载荷、位置上限和时间线阶段名称处理。`P=num_layers` 不再产生通信载荷、通信事件或云端时间；`P=0,K=0` 公开标记为 `Remote Target without Speculation`，并继续保留最小 Token 请求载荷。
- 显存模型按实际参与组件计费：`K=0` 不再装载草稿模型或草稿 KV，纯远程模式不再占用边缘框架/草稿内存，严格本地模式不再占用云端框架内存或通信缓冲区。
- 对实际参与的草稿和目标模型执行位置长度检查。草稿需要容纳 `prompt_len + K`，目标需要容纳 `prompt_len + K + 1`；越界动作返回零吞吐和可追溯的 `draft_position_limit`、`target_position_limit` 或组合原因。
- PPO 观测由四维 `[bandwidth_mbps, latency_ms, prompt_len, last_throughput]` 收缩为三维 `[bandwidth_mbps, link_delay_ms, prompt_len]`。域随机化、主性能和 PPO 评估场景固定在 `128/512/1024/1536`；`2048/4096/8192` 留给后续上下文边界审计。
- 论文实验和评估入口现在会对旧观测维度或未解析 Git LFS 的模型/`VecNormalize` 工件给出明确错误，而不是混用旧工件。

**采用的方法或措施**：所有吞吐、显存、时间线和位置可行性均继续经过 `engine/simulator.py`，环境和实验脚本未复制公式。新增 `test_p0_simulator_semantics.py` 覆盖严格本地、远程无投机、非投机激活切分和位置越界；新增 `test_p0_env_observation.py` 覆盖单步三维观测与模式标签。

**验证情况**：

- `python test_p0_simulator_semantics.py`：通过。
- `conda run -n heria python test_p0_simulator_semantics.py`：通过。
- `conda run -n heria python test_p0_env_observation.py`：通过。
- `conda run -n heria --no-capture-output python test_phase1.py`：通过。
- `conda run -n heria --no-capture-output python test_phase3.py`：通过，流水线吞吐仍高于顺序微批。
- `conda run -n heria --no-capture-output python test_phase4.py`：通过，环境观测空间为三维，Legacy 与 Token-ID 动作均可评估。
- `conda run -n heria --no-capture-output python -m compileall engine rl run_paper_experiments.py eval_phase5.py test_p0_simulator_semantics.py test_p0_env_observation.py`：通过。
- `conda run -n heria --no-capture-output python run_paper_experiments.py`：按预期在加载现有 `VecNormalize` 时停止。文件是未解析的 Git LFS 指针（`UnpicklingError: invalid load key, 'v'`）；入口已将其转换为可操作的说明。未把这次失败记为实验结果。

**仍存风险**：现有 PPO 模型及归一化工件无法用于新的三维观测契约，即使恢复 LFS 也必须按计划重训并使用对应的 `VecNormalize`。尚未重新生成论文数字、结构化结果或图表；这些是后续 P1/P2 工作，不能沿用旧数值。

**对应本地提交**：`011cb58`（修复P0仿真语义与PPO观测契约）；发布副本已推送为远程 `174c5ac`。

**下一步**：在可用的 `heria` 环境恢复所需 LFS 工件后，按种子 `42`、`20260912`、`20261127` 重训 PPO 并配对保存归一化文件；在此之前先实施 P1 的结构化结果追踪，确保重算输出可复现且不依赖硬编码图表数据。

### 2026-09-16 - P1 结构化结果追踪

**当前进度**：已完成 P0 代码发布到 `origin/main`（远程提交 `174c5ac`，确认不含 `models/`）。已完成论文实验入口的原始 JSON/CSV 结果追踪；本阶段未修改最终图表或论文正文。

**本次内容与实现方式**：新增无 Gym/PPO 依赖的 `result_tracking.py`，通过配置文件内容哈希、当前 Git 修订、总批大小、压力档、随机种子与 PPO/`VecNormalize` 工件路径和哈希建立运行元数据。`ResultRecorder` 为每条同一后端评估结果写入固定模式的 JSON 与 CSV，字段包含场景、动作索引和值、模式、可行性、OOM/位置原因、吞吐、makespan、Token/激活载荷、阶段成本、显存和配置哈希。`run_paper_experiments.py` 已接入该记录器，六组实验中的基线、PPO 和 Oracle 结果会统一输出到 `results/paper_experiments/paper_experiments.json` 与 `.csv`。

**采用的方法或措施**：记录器只接收 `DeepFlowEnv.evaluate_action()` 已产生的信息，未复制吞吐、显存或流水线公式。为避免缺失的模型产物被误写成实验数据，只有完成所有实验后才落盘结果；PPO 工件不可加载时实验入口保持失败，不生成部分或伪造的结果集。

**验证情况**：

- `conda run -n heria --no-capture-output python test_result_tracking.py`：通过，确认 JSON/CSV 具有同一固定字段、配置哈希、动作/模式和阶段成本。
- `conda run -n heria --no-capture-output python -m compileall result_tracking.py run_paper_experiments.py test_result_tracking.py`：通过。
- `run_paper_experiments.py` 仍未生成正式结果：P0 时确认的 `VecNormalize` LFS 指针/旧观测工件仍不可用。该前置条件未解决前不运行、也不提交任何模拟结果或图表。

**仍存风险**：目前图表脚本仍含历史硬编码数值，尚未读取新的结构化文件；必须在 P1 后续取得重训 PPO 和重新计算的 JSON/CSV 后才可改造，不能用旧论文数字填充新结果文件。三种子 PPO 重训和模型规模/压力实验尚未开始。

**对应本地提交**：`958c798`（建立论文实验结构化结果追踪）；发布副本已推送为远程 `6be2712`。

**下一步**：恢复或重新生成可用的 PPO/`VecNormalize` 配对工件后，运行论文实验产生首份结构化结果；随后将图表脚本改为仅读取该结果文件，并实施 Global Static、Heuristic 与 Per-scenario Oracle 的公平重构。

### 2026-09-16 - P1 策略基线公平重构（实施中）

**当前进度**：开始实现独立校准的 Global Static DeepFlow、冻结的 Heuristic DeepFlow 和全动作 Per-scenario Oracle。论文正文、回复信和最终图表保持未修改。

**影响范围与方法**：校准集固定为带宽 `{0.5,1,2,5,10,50,100}` Mbps、链路延迟 `{10,30,50,100,120}` ms、Prompt `{128,512,1024,1536}`；候选 Global Static 动作只使用 `P=0,K>0` 的 Token-ID DeepFlow 动作，在每个校准场景相对全动作 Per-scenario Oracle 的吞吐比上求平均，并以最小扁平动作索引处理并列。独立测试集固定为带宽 `{0.75,3,20,75}` Mbps、延迟 `{20,80}` ms、Prompt `{256,768,1280,1536}`。Heuristic 固定为预先声明的 `P=0,K=1` 分段 MB 规则，不根据测试结果调参。

**待验证**：Global Static 在相同校准集上重复选择必须确定；Heuristic 动作必须始终位于动作空间并遵循冻结阈值；论文实验记录必须区分 `global_static_deepflow`、`heuristic_deepflow` 与 `per_scenario_oracle`，且独立测试集不参与固定动作的校准。

**本次内容与实现方式**：新增 `policy_baselines.py`。`Per-scenario Oracle` 在当前场景枚举所有 `1188` 个动作，只作为上界；`Global Static DeepFlow` 仅枚举 `P=0,K>0` 的 30 个 Token-ID 动作，并按 140 个校准场景相对全动作 Oracle 的平均吞吐比选择一次固定动作。并列时保留最小扁平动作索引。`Heuristic DeepFlow` 固定为 `P=0,K=1`，短 Prompt 依据 50 ms 阈值选择 MB 4/8，中等 Prompt 依据 40/80 ms 阈值选择 MB 1/2/4，长 Prompt 固定 MB 1。`run_paper_experiments.py` 现在将这三者、严格本地、远程无投机、Legacy、无流水线 Token 投机和 PPO 分别记录，且 Global Static 校准元数据与独立测试集写入结构化结果。

**采用的方法或措施**：全部策略通过 `DeepFlowEnv.evaluate_action()` 调用 `DeepFlowSimulator`，没有在基线模块中实现第二套吞吐、显存或流水线计算。新增 `test_policy_baselines.py` 使用小型校准集验证固定动作选择确定、Token 候选只包含 `P=0,K>0`、冻结 Heuristic 阈值和全动作 Oracle；完整 140 场校准留给有效 PPO 工件到位后的整套论文实验。

**验证情况**：

- `conda run -n heria --no-capture-output python -m compileall policy_baselines.py result_tracking.py run_paper_experiments.py test_policy_baselines.py`：通过。
- `conda run -n heria --no-capture-output python test_policy_baselines.py`：通过。
- `conda run -n heria --no-capture-output python test_result_tracking.py`：通过，校准元数据扩展未改变结果记录模式。
- 未运行完整 `run_paper_experiments.py`：仍缺少可加载且与三维观测配对的 PPO/`VecNormalize` 工件。没有用旧模型、硬编码数值或不完整输出替代该验证。

**仍存风险**：全量 140 场 Global Static 校准和 32 场独立测试将在可用 PPO 工件后与 PPO 共同运行，可能改变原论文全部性能数值。PPO 尚未按 `42`、`20260912`、`20261127` 重训，因而目前不能比较或宣称 PPO 相对 Global Static/Heuristic 的收益。

**对应本地提交**：提交说明为“重构固定启发式与逐场景Oracle基线”（待本阶段提交完成）。

**下一步**：重新训练三种 PPO 种子并严格保存各自 `VecNormalize`，在独立测试集上生成结构化结果；只有满足预定义 Oracle 比和可行率门槛后，才评估 PPO 是否可主张优于固定/启发式策略。

### 2026-09-16 - P1 三种子 PPO 训练与独立测试重算（训练待启动）

**当前进度**：已完成训练与评估执行链改造并通过入口契约回归；下一步将在原始完整工作区训练 `42`、`20260912`、`20261127`。

**影响范围与方法**：每个种子将使用单独的模型、最佳模型、`VecNormalize`、检查点、TensorBoard 日志和训练元数据目录。独立测试重算将显式传入同一目录内的模型和归一化文件，输出包含工件路径与哈希的结构化结果；不允许回退到旧四维 PPO 或跨种子混用统计文件。

**本次内容与实现方式**：`train_phase5.py` 现在只接受预定义的三种训练种子，并将每次训练的最佳模型、最终模型、`VecNormalize`、检查点、TensorBoard 日志和 `training_metadata.json` 分离到 `models/ppo_deepflow/seeds/seed_<seed>/`。训练元数据记录配置哈希、Git 修订、三维观测契约和成对工件路径。`run_paper_experiments.py` 新增 `--model-path`、`--vec-normalize-path`、`--output-dir`、`--suite-name`、`--config-dir`、`--random-seed` 和 `--only-independent-test`；显式模型与归一化路径必须同时给出且均存在，避免回退到其它种子或历史四维工件。独立测试模式仍会先执行预定义 140 场 Global Static 校准，再只写入 32 场独立测试的策略结果。

**验证情况**：

- `conda run -n heria --no-capture-output python -m compileall train_phase5.py run_paper_experiments.py test_seeded_ppo_entrypoints.py`：通过。
- `conda run -n heria --no-capture-output python test_seeded_ppo_entrypoints.py`：通过，覆盖三种子参数、目录构造、显式模型/`VecNormalize` 成对约束和独立测试模式。
- `conda run -n heria --no-capture-output python test_policy_baselines.py`：通过。
- `conda run -n heria --no-capture-output python test_result_tracking.py`：通过。

**仍存风险**：尚未执行 350,000 步的正式训练或生成正式数值；不能基于本次代码修改更新论文结论。训练和评估工件会写入原始完整工作区的 `models/ppo_deepflow/seeds/` 与 `results/independent_test/`，其中模型二进制只能保留在本地 `main`，不得发布到远程代码分支。

**对应本地提交**：提交说明为“支持三种子PPO重训与独立测试重算”。

**下一步**：将本次无模型代码和记录同步至原始工作区，依次运行三个种子训练。每个训练完成后，使用该种子的 `best_model/best_model.zip` 和 `best_model/vec_normalize.pkl` 执行 `--only-independent-test`，生成独立目录的 JSON/CSV；三份结果齐全后再汇总平均吞吐、标准差、相对 Oracle 比和可行率。
